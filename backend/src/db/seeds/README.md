# Seed Plan

Planned seeds:

1. roles
2. initial admin user
3. topics and problems migrated from frontend static data

## Available Seed Scripts

1. `npm run seed:admin`
	- Creates or updates the default admin user from `.env` values.
